@model IEnumerable<TennApp.Models.ReportType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ReferenceCode)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ReferenceCode)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.ReportTypeID">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.ReportTypeID">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.ReportTypeID">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>